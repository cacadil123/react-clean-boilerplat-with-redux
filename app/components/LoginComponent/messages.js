/*
 * LoginComponent Messages
 *
 * This contains all the text for the LoginComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LoginComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LoginComponent component!',
  },
});
