/*
 * Login1 Messages
 *
 * This contains all the text for the Login1 container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Login1';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Login1 container!',
  },
});
